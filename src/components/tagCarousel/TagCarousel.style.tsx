import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  tagContainer: {
    borderRadius: 20,
    padding: 10,
    textAlign: 'center',
    marginRight: 10,
  },
  tagText: {
    fontSize: 12,
    fontWeight: '400',
    textAlign: 'center',
  },
});
